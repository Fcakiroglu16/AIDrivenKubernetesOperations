apiVersion: v1
kind: Service
metadata:
  name: app5-api
  namespace: app5-system
  labels:
    app.kubernetes.io/name: app5-api
    app.kubernetes.io/component: api
spec:
  # ClusterIP: internal only — expose externally via an Ingress resource
  type: ClusterIP
  selector:
    app.kubernetes.io/name: app5-api
  sessionAffinity: None
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: http
