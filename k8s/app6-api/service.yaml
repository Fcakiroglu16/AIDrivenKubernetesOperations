apiVersion: v1
kind: Service
metadata:
  name: app6-api
  namespace: app6-system
  labels:
    app.kubernetes.io/name: app6-api
    app.kubernetes.io/component: api
spec:
  # ClusterIP: internal only — expose externally via an Ingress resource
  type: ClusterIP
  selector:
    app.kubernetes.io/name: app6-api
  sessionAffinity: None
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: http
