apiVersion: apps/v1
kind: Deployment
metadata:
  name: app4-api
  namespace: app4-system
  labels:
    app.kubernetes.io/name: app4-api
    app.kubernetes.io/version: "1.0.0"
    app.kubernetes.io/component: api
spec:
  replicas: 2
  selector:
    matchLabels:
      app.kubernetes.io/name: app4-api
  strategy:
    type: RollingUpdate
    rollingUpdate:
      # maxUnavailable: 0 ensures zero-downtime rolling updates
      maxUnavailable: 0
      maxSurge: 1
  template:
    metadata:
      labels:
        app.kubernetes.io/name: app4-api
        app.kubernetes.io/version: "1.0.0"
        app.kubernetes.io/component: api
    spec:
      terminationGracePeriodSeconds: 60
      securityContext:
        runAsNonRoot: true
        seccompProfile:
          type: RuntimeDefault
      # Spread pods evenly across nodes to avoid a single point of failure
      topologySpreadConstraints:
        - maxSkew: 1
          topologyKey: kubernetes.io/hostname
          whenUnsatisfiable: DoNotSchedule
          labelSelector:
            matchLabels:
              app.kubernetes.io/name: app4-api
      containers:
        - name: app4-api
          # Built locally via: docker build -t app4-api:latest -f App4.API/Dockerfile .
          image: app4-api:latest
          imagePullPolicy: Never
          ports:
            - name: http
              containerPort: 8080
              protocol: TCP
          # Load all non-sensitive settings from ConfigMap and secrets from Secret
          envFrom:
            - configMapRef:
                name: app4-api-config
            - secretRef:
                name: app4-api-secret
          resources:
            requests:
              cpu: "100m"
              memory: "128Mi"
            limits:
              cpu: "500m"
              memory: "256Mi"
          livenessProbe:
            httpGet:
              path: /weatherforecast
              port: http
            initialDelaySeconds: 10
            periodSeconds: 15
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /weatherforecast
              port: http
            initialDelaySeconds: 5
            periodSeconds: 10
            failureThreshold: 3
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop: ["ALL"]
